services:
  bmc-analyzer:
    image: yuyeshun2/bmc-log-analyzer:v0.50rc1
    container_name: bmc-log-analyzer
    ports:
      - "8000:8000"
    # 可选：API 认证密钥（不配置则无需认证）
    # environment:
    #   - API_KEY=your-secret-key
    restart: unless-stopped
