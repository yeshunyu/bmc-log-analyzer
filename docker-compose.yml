services:
  bmc-analyzer:
    build:
      context: .
      args:
        APP_VERSION: ${APP_VERSION:-v0.32}
    container_name: bmc-log-analyzer
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/uploads:/app/app/uploads
    restart: unless-stopped
